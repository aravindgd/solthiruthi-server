$(".dictionary_table_span").html('#{escape_javascript(render "/dictionaries/dictionaries_table",dictionaries: @dictionaries)}');

$('.paginator').html('#{escape_javascript(paginate(@dictionaries, :remote => true))}
= succeed "');" do
  %br
$('.paginator').append('#{escape_javascript(page_entries_info @dictionaries)}');
